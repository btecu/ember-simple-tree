{{#if hasBlock}}
  {{#x-tree-node
    check=check
    select=select
    hover=hover
    hoverOut=hoverOut
    checkable=checkable
    chosenId=chosenId
    model=model as |node|}}
    {{yield node}}
  {{/x-tree-node}}

  {{#if model.isExpanded}}
    {{#x-tree-branch
      check=check
      select=select
      hover=hover
      hoverOut=hoverOut
      checkable=checkable
      chosenId=chosenId
      model=model.children as |node|}}
      {{yield node}}
    {{/x-tree-branch}}
  {{/if}}
{{else}}
  {{x-tree-node
    model=model
    check=check
    select=select
    hover=hover
    hoverOut=hoverOut
    checkable=checkable
    chosenId=chosenId}}

  {{#if model.isExpanded}}
    {{x-tree-branch
      model=model.children
      check=check
      select=select
      hover=hover
      hoverOut=hoverOut
      checkable=checkable
      chosenId=chosenId}}
  {{/if}}
{{/if}}
