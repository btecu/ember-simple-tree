{{#each model as |child|}}
  {{#if child.isVisible}}
    {{#if hasBlock}}
      {{yield child}}
    {{else}}
      {{x-tree-children model=child select=select hover=hover chosenId=chosenId}}
    {{/if}}
  {{/if}}
{{/each}}
