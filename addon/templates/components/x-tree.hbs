{{#if hasBlock}}
  {{#x-tree-branch
    select=select
    hover=hover
    hoverOut=hoverOut
    checkable=checkable
    syncCheckbox=syncCheckbox
    chosenId=chosenId
    model=model as |node|}}
    {{yield node}}
  {{/x-tree-branch}}
{{else}}
  {{x-tree-branch
    model=model
    select=select
    hover=hover
    hoverOut=hoverOut
    checkable=checkable
    syncCheckbox=syncCheckbox
    chosenId=chosenId}}
{{/if}}
