{{#if hasBlock}}
  {{#x-tree-branch
    model=model
    select=select
    hover=hover
    hoverOut=hoverOut
    checkable=checkable
    chosenId=chosenId}}
    {{yield}}
  {{/x-tree-branch}}
{{else}}
  {{x-tree-branch
    model=model
    select=select
    hover=hover
    hoverOut=hoverOut
    checkable=checkable
    chosenId=chosenId}}
{{/if}}
