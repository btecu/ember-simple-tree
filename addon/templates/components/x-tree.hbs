{{#each model as |child|}}
  {{#if child.isVisible}}
    {{#if hasBlock}}
      {{yield child}}
    {{else}}
      {{x-tree-children model=child select=select hover=hover}}
    {{/if}}
  {{/if}}
{{/each}}
